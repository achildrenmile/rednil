---
interface Props {
  class?: string;
}
const { class: className = '' } = Astro.props;
---
<span class:list={['flex items-center gap-2', className]}>
  <img src="/logo.png" alt="rednil" class="h-8 w-auto" />
</span>
